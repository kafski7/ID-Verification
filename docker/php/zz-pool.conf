[www]
; Boost worker count so parallel browser requests (HTML + Livewire poll + assets fallback) don't queue.
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8
pm.max_requests = 500
